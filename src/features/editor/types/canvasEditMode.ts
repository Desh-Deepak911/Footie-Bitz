/**
 * Presentation-only canvas interaction modes for the editor preview frame.
 * Derived by Selection Engine via selectCanvasEditMode.
 */
export type CanvasEditMode = "preview" | "frameEdit" | "playback";
