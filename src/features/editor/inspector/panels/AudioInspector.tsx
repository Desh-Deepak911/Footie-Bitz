/**
 * Future-only inspector panel for timeline audio clip selection.
 *
 * Not registered in `createEditorInspectorRegistry()` until audio targets resolve
 * in `resolveInspectorPanels()`. Project-level audio lives in `ProjectAudioStudio`.
 */
export default function AudioInspector() {
  return null;
}
